# toolkata Sandbox API - Environment Variables
# Copy this file to .env and fill in your values

# Server configuration
PORT=3001
HOST=0.0.0.0

# Frontend origin for CORS
# Development: http://localhost:3000
# Production: https://toolkata.com
FRONTEND_ORIGIN=http://localhost:3000

# Docker configuration
# Unix socket for Docker API (default: /var/run/docker.sock)
# DOCKER_HOST=/var/run/docker.sock

# Optional: Docker TLS configuration (for remote Docker)
# DOCKER_CERT_PATH=/path/to/cert.pem
# DOCKER_HOST=tcp://docker-host:2376
# DOCKER_TLS_VERIFY=1

# JWT Authentication (for tiered rate limiting)
# Must match AUTH_SECRET in packages/web/.env.local
# Without this, all users are treated as anonymous (base rate limits)
# AUTH_SECRET=your-secret-here

# Rate limiting (defaults shown)
# MAX_SESSIONS_PER_HOUR=50
# MAX_CONCURRENT_PER_IP=2
# MAX_WEBSOCKETS_PER_IP=3
# COMMANDS_PER_MINUTE=60

# GitHub CMS Configuration
# Required for Content CMS features (admin dashboard content editing)
# Without GITHUB_TOKEN, CMS features are disabled

# GitHub personal access token with `repo` scope
# Generate at: GitHub → Settings → Developer settings → Personal access tokens
# GITHUB_TOKEN=ghp_xxxxx

# Repository owner (user or organization)
# GITHUB_OWNER=toolkata

# Repository name
# GITHUB_REPO=content

# Default branch (optional, defaults to "main")
# GITHUB_DEFAULT_BRANCH=main
